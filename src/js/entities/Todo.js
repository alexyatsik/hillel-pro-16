'use strict';

class Todo {
    constructor(status, priority, task, description) {
        this.status = status;
        this.priority = priority;
        this.task = task;
        this.description = description;
    }
}